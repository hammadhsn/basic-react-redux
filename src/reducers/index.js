
import changeNumber from "./upDown";

import { combineReducers } from "redux";

// All the state is in rootReducer....

const rootReducer = combineReducers({
    changeNumber
})

export default rootReducer;